[pytest]
pythonpath = .
testpaths = tests
markers =
    real_world: benchmark over genuine third-party PPTX decks (skipped if corpus not fetched)
    real_world_v2: PR6.1 business-archetype benchmark over third-party PPTX decks (skipped if corpus not fetched)
